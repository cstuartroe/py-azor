isEven(n) = n % 2 == 0

power(base, exp) = if exp == 0
                     then 1
                   else if isEven(exp)
                     then power(base * base, exp / 2)
                   else
                     base * power(base, exp - 1)

main() = power(5, 7)
